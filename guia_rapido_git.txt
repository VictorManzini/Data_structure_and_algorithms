Tem aqui um guia rapido de como criar um novo repositório no Git e linkar no GitHub:

GITHUB:
Crie um repositório no GitHub com o nome da pasta cou do projeto. 
Crie o repositório sem o README, LICENSE ou .gitignore, tudo isso será adicionado depois.
Para adicnioar uma licença ao repositório, vai no GitHub e cria um arquivo novo, nomeie-o de LICENSE, 
você vai ver pouco abaixo de onde você digitou o nome LICENSE um botão para adicionar uma licença.


GIT:
git init
git add .
git commit -m "primeiro commit"
git remote add origin https://github.com/VictorManzini/nome-da-materia.git (aqui é do trocar a ulr para a ulr real
do repositório do GitHub)
git push --set-upstream origin main
*Toda vez que você abrir uma pasta ja criada você precisa fazer o push com git push --set-upstream origin main 
antes de fazer apenas o git push 

Quando foi adicionar um arquivo que ja foi comitado no .gitignore precisa rodar esse comando: git rm -cached <pasta e 
nome do arquivo> 
Exemplo: git rm --cached projetos_c/Exercicios_pessoais/notas

Branches: 
Para criar e alternar para uma nova branch: git checkout -b nome-da-branch
ou para versões do git acima da 2.23 usar git switch -c nome-da-branch
Para apenas criar uma nova branch: git branch nome-da-branch
Para alternar para umaa branch já existente: git checkout nome-da-branch
Algumas recomendações: 
1-Atualize a branch principal: git checkout main e git pull origin main.
2-Crie e entre na nova branch: git push -u origin feature/nome-da-feature.
3-Envie a branch para o repositório remoto: git push -u origin feature/nome-da-feature.
